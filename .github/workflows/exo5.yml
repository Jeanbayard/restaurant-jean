name: CI5

on:
  push:
    branches: [main]
  
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test: [npm.test, npm.test, npm.test]

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Installer les dépendances
        run: npm install

      - name: Executer lint
        run: npm run lint

      - name: Lance les testes
        run: ${{ matrix.test  }}


